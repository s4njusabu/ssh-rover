[package]
name = "ssh-rover"
version = "0.1.3"
edition = "2024"
description = "A terminal user interface (TUI) for discovering hosts on a local network and connecting to them over SSH."
license = "MIT"
repository = "https://github.com/s4njusabu/sshrover"
readme = "README.md"

[dependencies]
crossterm = "0.29.0"
ipnet = "2.12.0"
ratatui = "0.30.2"
